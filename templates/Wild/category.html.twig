{% extends 'base.html.twig' %}

{% block title %}{{ categoryName }}{% endblock %}

{% block body %}

    <div class="container-fluid d-flex">
        <h3><a href="{{ path('wild_index') }}" class="backSeries"><span>S</span>éries > </a></h3>
        <h3 class="ml-3">{{ category.name }}</h3>
    </div>

    <div class="container-fluid">
        <div class="row">

            {% for program in programs %}
                <div class="col-lg-2 col-6  d-flex justify-content-around">
                    <div class="card mt-3" style="width: 10rem; height: 14rem;">
                        <a href="{{ path('show_program', {'slug': program.slug }) }}">
                            <img src="{{ program.poster }}" class="card-img-top" alt="..."
                                 style="width: 10rem; height: 14rem;">
                        </a>
                    </div>
                </div>
            {% else %}
                Aucune série trouvée
            {% endfor %}

        </div>
    </div>

{% endblock %}
