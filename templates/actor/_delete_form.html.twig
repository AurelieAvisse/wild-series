<form method="post" action="{{ path('actor_delete', {'id': actor.id}) }}"
      onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet acteur ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actor.id) }}">
    <button class="btn p-0"><i class='fa fa-trash icon-actor delete'></i></button>
</form>
