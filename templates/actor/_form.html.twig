{{ form_start(form) }}
<div class="name_actor mb-lg-3 mb-md-5 mt-lg-4 mt-md-5">
    {{ form_row(form.name) }}
</div>

<button class="button mt-3">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
