{% extends 'base.html.twig' %}

{% block title %}Edit Actor{% endblock %}

{% block body %}

    <div class="container mt-4">
        <h2>Modifier l'acteur</h2>

        <h3>{{ actor.name }}</h3>

        {{ include('actor/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="mt-4">
            <a href="{{ path('actor_new') }}" class="backProgram">
                <i class='fa fa-mail-reply mr-2'></i>
                Retour à la liste des acteurs</a>
        </div>

    </div>
{% endblock %}
