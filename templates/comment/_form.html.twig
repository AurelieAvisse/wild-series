{{ form_start(form) }}

<div class="rate">Vote:</div>
<div class="stars">
    {{ form_row(form.rate) }}
</div>

<div class="comment mt-1 mb-2">Ajouter votre commentaire:</div>
{{ form_row(form.comment) }}

<button class="button mt-3 mb-5">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
