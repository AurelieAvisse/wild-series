{{ form_start(form) }}
<div id="episode_title">
    {{ form_row(form.title) }}
</div>
<div id="episode_number" class="mt-3">
    {{ form_row(form.number) }}
</div>
<div id="episode_synopsis" class="mt-3">
    {{ form_row(form.synopsis) }}
</div>

<button class="btnCategories mt-3">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}

