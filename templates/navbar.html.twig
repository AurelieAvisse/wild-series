<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('app_index') }}">Wild<span>Series</span></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">

        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link active" href="{{ path('app_index') }}">Accueil <span
                            class="sr-only">(current)</span></a>
            </li>
            <li>
                <a class="nav-item nav-link" href="{{ path('wild_index') }}">Séries</a>
            </li>
        </ul>

        <ul class="navbar-nav mr-4">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">
                    <i class='fa fa-plus fa-plus-navbar mr-2' style='font-size:1em;color:#00fff7'></i>
                    Options
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('category_new') }}">Ajouter une catégorie</a>
                    <a class="dropdown-item" href="{{ path('program_index') }}">Ajouter une série</a>
                    <a class="dropdown-item" href="{{ path('season_index') }}">Ajouter une saison</a>
                    <a class="dropdown-item" href="{{ path('episode_index') }}">Ajouter un épisode</a>
                </div>
            </li>
        </ul>

        {% if app.user %}
            <ul class="navbar-nav mr-4">
                <li class="nav-item active">
                    <a class="nav-link disabled" href="">
                        <i class='fa fa-user mr-1' style='font-size:1em;color:#00fff7'></i>
                        {{ app.user.email }}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">
                        <i class='fa fa-unlink mr-1' style='font-size:1em;color:#00fff7'></i>
                        Déconnexion
                    </a>
                </li>
            </ul>

        {% else %}
            <ul class="navbar-nav mr-4">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">
                        <i class='fa fa-unlink mr-1' style='font-size:1em;color:#00fff7'></i>
                        Se connecter
                    </a>
                </li>
            </ul>
        {% endif %}

    </div>
</nav>