{{ form_start(form) }}
<div id="season_year">
    {{ form_row(form.year) }}
</div>
<div id="season_description" class="mt-3">
    {{ form_row(form.description) }}
</div>

<button class="btnCategories mt-3">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
