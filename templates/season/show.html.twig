{% extends 'base.html.twig' %}

{% block title %}Saisons {{ season.program.title }}{% endblock %}

{% block body %}
    <div class="container-fluid mt-4">

        <div class="d-flex mt-3">
            <h3 class="mt-0">{{ season.program.title }}</h3>
            <a href="{{ path('season_edit', {'id': season.id}) }}"
               class="mx-lg-4 mx-md-5">
                <i class='fa fa-pencil icon-crud'></i>
            </a>
            {{ include('season/_delete_form.html.twig') }}
        </div>

        <h4 class="mt-lg-4 mt-md-5">Saison: <span class="info-season-crud ml-2">{{ season.year }}</span></h4>
        <h4 class="my-lg-4 my-md-5">Description: <span class="info-season-crud ml-2">{{ season.description }}</span>
        </h4>

        <div class="mt-4">
            <a href="{{ path('seasons_index', {'id': season.program.id}) }}" class="back_crud">
                <i class='fa fa-mail-reply mr-2'></i>
                Retour à la liste des saisons</a>
        </div>
    </div>

{% endblock %}
